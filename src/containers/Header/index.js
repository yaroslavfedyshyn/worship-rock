import React from "react";

import Menu from './Menu';

const Header = (props) => {

    return (
        <header className="header-area">
            <Menu/>
        </header>
    )
};


export default Header;